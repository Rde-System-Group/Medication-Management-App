{
    "routes": [
        {
            "route": "/api/rest/*",
            "methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
            "allowedRoles": ["anonymous"]
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": ["/rest/*"]
    },
    "forwardingGateway": {
        "requiredHeaders": {},
        "allowedForwardedHosts": []
    },
    "platform": {
        "apiRuntime": "static"
    },
    "routes": [
        {
            "route": "/api/rest/*",
            "rewrite": "/rest/*"
        }
    ]
}